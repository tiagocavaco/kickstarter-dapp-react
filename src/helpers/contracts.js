import CampaignFactory from '@ethereum/.deploy/CampaignFactory.json';
import Campaign from '@ethereum/.build/Campaign.json';

export const contracts = {
  campaignFactory: CampaignFactory,
  campaign: Campaign
}